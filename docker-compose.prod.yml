services:
  scout:
    build: .
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./reports:/app/reports
      - ./publications:/app/publications
    networks:
      - scout-network
    restart: "no"

networks:
  scout-network:
    driver: bridge
